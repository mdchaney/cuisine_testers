<%= form_for(@tester) do |f| %>
  <% if @tester.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tester.errors.count, "error") %> prohibited this tester from being saved:</h2>

      <ul>
      <% @tester.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :cuisine_ids %><br />
    <%= f.collection_select :cuisine_ids, Cuisine.all, :id, :name, {}, { multiple: true, size: 5 } %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
