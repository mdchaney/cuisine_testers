<%= form_for(@cuisine) do |f| %>
  <% if @cuisine.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cuisine.errors.count, "error") %> prohibited this cuisine from being saved:</h2>

      <ul>
      <% @cuisine.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :tester_ids %><br />
    <%= f.collection_select :tester_ids, Tester.all, :id, :name, {}, { multiple: true, size: 5 } %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
